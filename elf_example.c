#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv) {
    if (argc==3){
        printf("printing from elf_example!\n");
        int a = atoi("%s",*argv[i]);
        int b = atoi("%s",*argv[i]);
        return (a + b);
    }
    return -1;
}
